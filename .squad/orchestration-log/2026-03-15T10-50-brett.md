# Orchestration Log — Brett (Infra)

**Date:** 2026-03-15T10:50 UTC  
**Agent:** Brett (Infra)  
**Mode:** background  
**Status:** Completed

## Output

**File:** `.squad/decisions/inbox/brett-hardening-spec.md`

### Summary

Production-grade Docker Compose hardening specification for #52. Covers 3 service tiers (infra, application, init) with comprehensive health checks, restart policies, resource limits, graceful shutdown, dependency fixes, and production deployment guide.

**Key Design Decisions:**
- **Health Checks:** 8 new health checks + 5 existing fixes (service_started → service_healthy)
- **Restart Policies:** `unless-stopped` for stateful/critical services (redis, rabbitmq, zoo ensemble, solr-search, embeddings, UIs)
- **Resource Limits:** Memory limits (256m-2g) + CPU reservations (0.5-1.0 core) + log rotation (10m × 3 files)
- **Graceful Shutdown:** 60s grace for stateful (Solr, ZooKeeper), 30s for queues/cache (RabbitMQ, Redis)
- **Dependency Graph:** Fixed 5 depends_on conditions (embeddings-server, solr-search, aithena-ui, nginx)
- **Critical Fix:** embeddings-server port conflict (remove PORT=8085 env, standardize on 8080 internal)

### Order of Implementation

1. Fix embeddings-server port conflict
2. Add /health endpoints (nginx, solr-search, embeddings-server)
3. Add health checks to all services
4. Update restart policies + resource limits + graceful shutdown
5. Fix depends_on conditions
6. Create docs/deployment/production.md

### Key Risks Mitigated

✅ Health check false positives — start_period set appropriately  
✅ Resource limits too tight — 2-2.5x observed usage  
✅ Graceful shutdown incomplete — 60s for stateful services  
✅ Log disk fill — rotation configured (10m × 3 files)

### Status for Downstream

⏳ Ready for @copilot implementation (after Groups 1-5 complete)  
✅ Brett review gate for pre-merge validation

---

**Next:** Assign #52 to @copilot with `squad:copilot` + `squad:brett` labels (after Groups 1-5).
