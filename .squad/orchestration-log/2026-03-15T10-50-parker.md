# Orchestration Log — Parker (Backend)

**Date:** 2026-03-15T10:50 UTC  
**Agent:** Parker (Backend)  
**Mode:** background  
**Status:** Completed

## Output

**File:** `.squad/decisions/inbox/parker-upload-endpoint-spec.md`

### Summary

Detailed API specification for `POST /v1/upload` endpoint (#49). Covers full contract (multipart/form-data, file validation, RabbitMQ integration), error handling, configuration, and test requirements.

**Key Design Decisions:**
- **Response Code:** 202 Accepted (async indexing)
- **File Validation:** Triple check (MIME type + extension + magic number)
- **Size Limit:** 50 MB (configurable via env)
- **Storage:** `/data/documents/uploads/` (shared volume with indexer)
- **Queue Integration:** Publishes to `shortembeddings` RabbitMQ queue (reuses existing indexer)
- **Connection Pattern:** Per-request pika connections (thread-safe, avoids singleton issues with uvicorn workers)
- **Dependencies:** pika, python-multipart, retry
- **Test Coverage:** ≥8 unit tests (validation, queue, error cases)

### Key Concerns Resolved

✅ Concern 1: Shared volume dependency — documented, startup validation added  
✅ Concern 2: RabbitMQ thread safety — per-request pattern recommended  
⚠️ Concern 3: Health check missing — recommended separate issue (out of #49 scope)

### Status for Downstream

✅ Ready for @copilot implementation (after approval from Ripley's plan)

---

**Next:** Assign #49 to @copilot with `squad:copilot` + `squad:parker` labels.
