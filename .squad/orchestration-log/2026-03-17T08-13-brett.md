# Brett (Infrastructure Architect) — 2026-03-17T08-13Z

**Orchestrated by:** User spawn manifest  
**Mode:** Background agent  
**Task:** Diagnose cascading Docker Compose failures (RabbitMQ, ZooKeeper, Solr timing)

## Summary

Diagnosed three independent root causes compounding into full-stack outage. Applied fixes to docker-compose.yml. RabbitMQ stale volume remains as operational issue (requires host-level cleanup).

## Identified Issues

1. **RabbitMQ Auth Failure** — Stale `/source/volumes/rabbitmq-data` volume with old credentials (guest/guest from prior init). `RABBITMQ_DEFAULT_USER/PASS` env vars only effective on first init.
2. **ZooKeeper Health Check Noise** — `mntr` 4LW command + grep causing SIGPIPE on exit (logs "broken pipe" errors, but health checks pass correctly).
3. **Books Collection Timing** — `solr-init` container has no compose dependency ordering; document-indexer starts polling before Solr is ready.

## Code Changes

- **ZooKeeper health checks (docker-compose.yml):** Simplified zoo1/zoo2/zoo3 from `ruok + mntr leader/follower grep` → `ruok`-only.
- **Document-indexer depends_on (docker-compose.yml):** Added `solr: condition: service_healthy` to prevent premature polling.

## Files Modified

- `docker-compose.yml`

## Decision Artifact

- `.squad/decisions/inbox/brett-infra-diagnosis.md`

## Outcome

✅ Code fixes applied. RabbitMQ volume issue identified as operational (requires `sudo rm -rf /source/volumes/rabbitmq-data/*` and restart).

---

**Created by Scribe**  
**Timestamp:** 2026-03-17T08-13Z
