# Brett — E2E Container Health Check Investigation (v1)

**Agent:** Brett (Infrastructure Architect)  
**Date:** 2026-03-16T16:28:00Z  
**Task:** Fix E2E test failures via docker-compose health check tuning  
**Status:** ⚠️ PARTIAL (Needs v2 Investigation)

## Outcome

### Initial Hypothesis
- **Theory:** Health checks timing out before services ready (10s start_period too short)
- **Action:** Increased start_period from 10s → 30s in docker-compose.yml
- **Result:** PR #357 created, but E2E still fails

### Problem Discovery
During PR #357 review, discovered:
- ❌ Container crashes on startup (not just slow health check)
- ❌ start_period adjustment alone insufficient
- ❌ Root cause appears deeper (likely dependency/initialization issue)

### Failure Mode
```
E2E test failure:
├── docker-compose up --build
├── Container starts
├── Health check fails (connection refused)
├── Container crashes (exit code 1)
└── E2E test fails
```

### What's NOT the Issue
- ✅ Health check timing (increased to 30s, still fails)
- ✅ Port binding (ports are exposed)
- ✅ Network connectivity (other services work)

### What IS the Issue
- Service initialization failing before health check runs
- Likely: Missing dependency, environment var, or startup sequence
- Requires detailed container logs and startup trace

## PR Status

| PR | Type | Target | Status | Outcome |
|----|------|--------|--------|---------|
| #357 | Fix | dev | OPEN | ⚠️ Partial (needs v2) |

## Recommended Next Steps

1. **Verbose Logging** — Run docker-compose with debug logs
2. **Startup Trace** — Capture STDOUT/STDERR from failing service
3. **Dependency Audit** — Verify all required services started
4. **Environment Check** — Confirm all env vars present at runtime
5. **Sequential Start** — Test individual service startup in isolation

## Follow-up Actions

- [ ] Assign to Brett v2 (deeper investigation)
- [ ] Gather container startup logs
- [ ] Identify root cause of crash
- [ ] Create follow-up PR with actual fix

## References

- Issue: #356 (P0 blocker)
- PR: #357 (health check timing, v1)
- Status: ⚠️ PARTIAL (escalate to v2)
- E2E CI: `.github/workflows/e2e.yml` (pre-existing infra issue)
