# PR #883 Merged

**By:** Ash (Search Engineer)  
**PR:** #883 (Embeddings Server input_type Parameter)  
**Status:** MERGED to dev  
**Timestamp:** 2026-03-22T11:44:59Z

## Summary

Fixed 3 review comments from PR #883:
1. **input_type default acknowledged** — Confirmed that `input_type` defaults to `"passage"` when omitted, correct for document indexing workflows
2. **model_utils.py extraction for test isolation** — Refactored shared model detection logic into `model_utils.py` (single source of truth) to avoid duplication and improve test isolation
3. (Implicit) **Prefix logic centralization** — Embeddings server now detects e5-family models and auto-applies `"query: "` / `"passage: "` prefixes based on `input_type`; callers pass intent, not prefix

Comment 2 required code extraction for better testability; comment 3 was already handled by the PR design.

## Impact

- `embeddings-server` can now serve both e5 and non-e5 models with correct prefix handling
- `solr-search` and `document-indexer` can request embeddings with semantic intent (`input_type`) rather than model-specific prefixes
- `model_utils.py` is now the single source of model family detection — simpler to test and extend
- Backward compatible — existing callers omitting `input_type` get safe default (`"passage"`)
