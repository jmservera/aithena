# Orchestration Log — Lambert (Tester)

**Spawn Time:** 2026-03-13T18:44  
**Agent:** Lambert (Tester)  
**Mode:** background  
**Model:** claude-sonnet-4.5  
**Status:** COMPLETED ✅

## Task Assignment

Write TDD tests for metadata extraction (Phase 1.5)

## Outcome

**SUCCESS**

### Deliverables

✅ Created pytest suite for metadata extraction:
- `document-indexer/tests/test_metadata.py` — 15 tests written
- `document-indexer/tests/conftest.py` — pytest fixtures and helpers
- `document-indexer/tests/__init__.py` — package marker
- `document-indexer/pytest.ini` — test configuration

**Test Results:** 11 passing, 4 failing (intentional — flagging real parser bugs)

### Test Coverage

**Passing (11):**
- Standard patterns: `Author/Title.pdf`, `Category/Author - Title (Year).pdf`
- Real library patterns: `amades/` author folder, `balearics/` category folder
- Edge cases: filenames with underscores, unknown folder patterns
- Fallback behavior: conservative title/author/unknown defaults

**Failing (4):** Intentional test failures exposing parser gaps:
- Fallback handling for deeply nested unsupported paths
- Year range parsing edge cases (e.g., `1885 - 1886` should stay in title)
- Category detection logic refinements needed

### Files Created

- `document-indexer/tests/test_metadata.py`
- `document-indexer/tests/conftest.py`
- `document-indexer/tests/__init__.py`
- `document-indexer/pytest.ini`

## Context

Implements Phase 1.5 QA checkpoint. Tests encode the contract between metadata extraction and Solr field population, ensuring stability for downstream faceting and search tuning (Phase 2).

**Contracts enforced:**
1. `file_path` and `folder_path` are relative to `base_path`
2. Real library conventions: `amades/` = author, `balearics/` = category, `bsal/` = journal
3. Unknown patterns stay conservative (title = filename, author = Unknown)
4. Year ranges not confused with `author - title` syntax

**Blockers for:**
- Phase 2 search API (depends on metadata stability)
- Phase 3 embeddings (depends on correct title/author for chunking)

**Next steps for Phase 1.5:**
- Fix 4 failing tests (parser refinements)
- Validate real library paths with full indexing
