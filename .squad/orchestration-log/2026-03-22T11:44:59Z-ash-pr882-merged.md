# PR #882 Merged

**By:** Ash (Search Engineer)  
**PR:** #882 (E5-base Solr Collection)  
**Status:** MERGED to dev  
**Timestamp:** 2026-03-22T11:44:59Z

## Summary

Fixed 4 review comments from PR #882:
1. **grep exact match** — Changed `grep` calls to use `-w` flag for exact word matching in Solr initialization script validation
2. **README update** — Updated `README.md` to document the new `books_e5base` Solr collection and its role in the A/B test
3. **prod yml postponed** — Acknowledged production `docker-compose.prod.yml` overlay won't include e5 services during testing phase; documented as future work
4. **test import acknowledged** — Confirmed test file imports are properly isolated; no additional changes needed

All comments resolved and merged.

## Impact

- `solr-init` script now validates schema paths with exact word matching (no false positives)
- Team and new users can understand the dual-collection A/B architecture from the README
- Production deployment remains stable — e5 test services not exposed in prod config
