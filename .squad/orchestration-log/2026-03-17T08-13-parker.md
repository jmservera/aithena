# Parker (Backend Dev) — 2026-03-17T08-13Z

**Orchestrated by:** User spawn manifest  
**Mode:** Background agent  
**Task:** Debug Redis auth wiring in Python services; verify credential propagation

## Summary

Found and fixed missing Redis password in solr-search ConnectionPool. Verified all other services correctly pass credentials. All 193 solr-search tests pass.

## Issue Found

**solr-search Redis ConnectionPool missing password:** The `_get_redis_pool()` function created a `redis.ConnectionPool()` without passing `password=settings.redis_password`, even though the password was correctly loaded from `REDIS_PASSWORD` env var. This caused silent auth failures against password-protected Redis.

## Code Changes

- **src/solr-search/main.py:854** — Added `password=settings.redis_password` to ConnectionPool constructor.

## Verification

- All 193 solr-search tests pass after change.
- Backward-compatible: redis-py ignores password when `None`.
- Other services verified correct (admin, document-lister, document-indexer).
- RabbitMQ `ACCESS_REFUSED` not a code bug — stale volume issue (operational).

## Files Modified

- `src/solr-search/main.py`

## Decision Artifact

- `.squad/decisions/inbox/parker-redis-diagnosis.md`

## Outcome

✅ Redis auth fixed. No related failures in other services. Ready for integration testing.

---

**Created by Scribe**  
**Timestamp:** 2026-03-17T08-13Z
