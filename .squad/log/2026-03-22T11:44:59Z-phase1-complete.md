# Phase 1 Completion — Embedding Model A/B Test

**Timestamp:** 2026-03-22T11:44:59Z  
**Status:** 3 of 5 Items Complete  
**Milestone:** v1.11.1 (P1-*)

## Phase 1 Infrastructure Setup

### ✅ COMPLETE

1. **P1-1:** E5 prefix handling in embeddings-server  
   - PR #883 merged (Ash)
   - Server detects model family and applies `"query: "` / `"passage: "` prefixes internally
   - Callers pass `input_type` intent
   - Backward compatible with existing callers

2. **P1-2:** Dual-collection Solr schema (`books` + `books_e5base`)  
   - PR #882 merged (Ash)
   - `books_e5base` is a full independent copy of `books` configset with e5-compatible vector field (768D HNSW)
   - Allows independent schema evolution post-test

3. **P1-5:** Collection parameter API & allowlist routing  
   - PR #884 merged (Parker)
   - `solr-search` endpoints accept optional `collection` param
   - Env-var-driven allowlist and per-collection config
   - Ready for frontend A/B UI

### 🔄 IN PROGRESS / TODO

4. **P1-3/P1-4:** Dual-indexing message routing (OQ-1, BLOCKING)  
   - RabbitMQ competing consumers issue must be resolved before dual-indexing setup
   - Brett + Ash to implement fanout exchange or separate queues
   - Blocks document-indexer-e5 deployment

5. **P1-6/P1-7:** Embeddings server e5 instance & env overlay  
   - Docker Compose e5 overlay services not yet defined
   - Awaits Phase 2 readiness; depends on message routing (P1-3/4)

## Technical Wins

- **Backward compatibility:** All Phase 1 changes are backward compatible
- **Clean separation:** E5 services isolated in Docker overlay, not in production config
- **Model-agnostic API:** Prefix logic centralized in embeddings-server; clients use semantic intent
- **Test infrastructure:** Solr collections and search API ready for dual-model queries

## Blockers

**OQ-1 (BLOCKING):** RabbitMQ competing consumers means only one indexer receives each message. Proposed solutions:
- Fanout exchange (separate queue per indexer)
- Duplicate RabbitMQ declaration in `document-indexer-e5` service
- Manual replay mechanism for Phase 2 bootstrapping

## Next Phase Gate

**Ready for Phase 2?** Yes, pending OQ-1 resolution. Ash and Brett should unblock this before Phase 2 indexing begins.

**PO confirmation pending:** CHUNK_SIZE recommendation (300/50 proposed for 512-token window) — OQ-2.
