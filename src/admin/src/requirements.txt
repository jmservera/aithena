# DEPRECATED: This file is no longer maintained.
# Use `admin/pyproject.toml` with `uv` instead:
#   cd admin && uv sync
#   uv run streamlit run src/main.py
streamlit>=1.51.0,<2
redis==7.4.0
python-dotenv==1.2.2
requests>=2.31.0
pillow>=12.1.1
