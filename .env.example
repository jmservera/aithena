# Path to your book library on the host
BOOKS_PATH=/home/jmservera/booklibrary

# Compatibility alias for installer consumers
BOOK_LIBRARY_PATH=/home/jmservera/booklibrary

# Public origin used by the browser-facing UI and API
PUBLIC_ORIGIN=http://localhost
CORS_ORIGINS=http://localhost

# Persistent auth database and JWT settings (the installer will generate the secret)
AUTH_DB_DIR=/home/your-user/.local/share/aithena/auth
AUTH_DB_PATH=/data/auth/users.db
AUTH_JWT_SECRET=generate-with-installer
AUTH_JWT_TTL=24h
AUTH_COOKIE_NAME=aithena_auth
AUTH_ADMIN_USERNAME=admin

# Container build metadata (buildall.sh exports these automatically)
VERSION=0.8.0
GIT_COMMIT=unknown
BUILD_DATE=1970-01-01T00:00:00Z
