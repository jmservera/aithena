services:
  redis:
    # it is importnant to configure OS memory overcommit
    # https://github.com/nextcloud/all-in-one/discussions/1731
    # run this in your VM:
    # echo "vm.overcommit_memory = 1" | sudo tee /etc/sysctl.d/nextcloud-aio-memory-overcommit.conf
    image: redis:7.4-alpine
    command: >-
      sh -c 'exec redis-server /usr/local/etc/redis/redis.conf
      --requirepass "$$REDIS_PASSWORD"'
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - REDISCLI_AUTH=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
    expose:
      - 6379
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 15s
      retries: 1
    restart: unless-stopped
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      default:
        aliases:
          - redis
    volumes:
      - redis-data:/data
      - ./src/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
  rabbitmq:
    image: rabbitmq:4.0-management
    entrypoint: /etc/rabbitmq/init-definitions.sh
    command: rabbitmq-server
    hostname: rabbitmq
    environment:
      # Admin user created on first Mnesia init (replaces default guest user)
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_ADMIN_USER:-admin}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_ADMIN_PASS:-admin_dev_pass}
      # Per-service passwords --- init-definitions.sh reads these to generate
      # /etc/rabbitmq/definitions.json at startup (see load_definitions in rabbitmq.conf)
      - RABBITMQ_LISTER_PASS=${RABBITMQ_LISTER_PASS:-lister_dev_pass}
      - RABBITMQ_INDEXER_PASS=${RABBITMQ_INDEXER_PASS:-indexer_dev_pass}
      - RABBITMQ_SEARCH_PASS=${RABBITMQ_SEARCH_PASS:-search_dev_pass}
      - RABBITMQ_ADMIN_PASS=${RABBITMQ_ADMIN_PASS:-admin_dev_pass}
    expose:
      - 5672
      - 15672
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
      - ./src/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
      - ./src/rabbitmq/init-definitions.sh:/etc/rabbitmq/init-definitions.sh:ro

    healthcheck:
      test: ["CMD", "rabbitmqctl", "ping"]
      interval: 10s
      timeout: 30s
      retries: 12
      start_period: 30s
    restart: unless-stopped
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          memory: 1g
        reservations:
          memory: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      default:
        aliases:
          - rabbitmq
  embeddings-server:
    build:
      context: ./src/embeddings-server
      args:
        VERSION: ${VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
        HF_TOKEN: ${HF_TOKEN:-}
    expose:
      - "8080"
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    restart: unless-stopped
    stop_grace_period: 10s
    deploy:
      resources:
        limits:
          memory: 3g
        reservations:
          memory: 2g
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      default:
        aliases:
          - embeddings-server
  document-lister:
    build:
      context: ./src/document-lister
      args:
        VERSION: ${VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    environment:
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_USER=${RABBITMQ_LISTER_USER:-lister}
      - RABBITMQ_PASS=${RABBITMQ_LISTER_PASS:-lister_dev_pass}
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - QUEUE_NAME=shortembeddings
      - EXCHANGE_NAME=documents
      - BASE_PATH=/data/documents/
      - DOCUMENT_WILDCARD=*.pdf
      - POLL_INTERVAL=60  # seconds between library scans (default: 60)
    depends_on:
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "pgrep", "-f", "python"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: on-failure
    stop_grace_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    volumes:
      - document-data:/data/documents
    networks:
      - default
  # Document indexer: e5-base embeddings → books collection
  document-indexer:
    build:
      context: ./src/document-indexer
      args:
        VERSION: ${VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    environment:
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_USER=${RABBITMQ_INDEXER_USER:-indexer}
      - RABBITMQ_PASS=${RABBITMQ_INDEXER_PASS:-indexer_dev_pass}
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - QUEUE_NAME=shortembeddings
      - EXCHANGE_NAME=documents
      - BASE_PATH=/data/documents/
      - SOLR_HOST=solr
      - SOLR_PORT=8983
      - SOLR_COLLECTION=books
      - EMBEDDINGS_HOST=embeddings-server
      - EMBEDDINGS_PORT=8080
      - CHUNK_SIZE=300
      - CHUNK_OVERLAP=50
      - SOLR_AUTH_USER=${SOLR_ADMIN_USER:-solr_admin}
      - SOLR_AUTH_PASS=${SOLR_ADMIN_PASS:-SolrAdmin_dev2024!}
    restart: on-failure
    stop_grace_period: 10s
    deploy:
      replicas: 1
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      embeddings-server:
        condition: service_healthy
      solr:
        condition: service_healthy
      solr-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "pgrep", "-f", "python"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - document-data:/data/documents:ro
    networks:
      - default
  # solr-search: unified search API supporting keyword (Solr BM25), semantic (Solr kNN HNSW),
  # and hybrid (RRF) modes.  Set DEFAULT_SEARCH_MODE=keyword|semantic|hybrid.
  solr-search:
    build:
      context: .
      dockerfile: ./src/solr-search/Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    expose:
      - "8080"
    restart: unless-stopped
    stop_grace_period: 10s
    depends_on:
      solr:
        condition: service_healthy
      embeddings-server:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
          cpus: "0.5"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - PORT=8080
      - SOLR_URL=http://solr:8983/solr
      - SOLR_COLLECTION=books
      - SOLR_ASCII_FOLDING=true
      - BASE_PATH=/data/documents
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost}
      - EMBEDDINGS_URL=http://embeddings-server:8080/v1/embeddings/
      - DEFAULT_SEARCH_MODE=keyword
      - UPLOAD_DIR=/data/documents/uploads
      - MAX_UPLOAD_SIZE_MB=50
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USER=${RABBITMQ_SEARCH_USER:-search}
      - RABBITMQ_PASS=${RABBITMQ_SEARCH_PASS:-search_dev_pass}
      - RABBITMQ_QUEUE_NAME=shortembeddings
      - E5_COLLECTIONS=books
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - AUTH_DB_PATH=${AUTH_DB_PATH:-/data/auth/users.db}
      - AUTH_JWT_SECRET=${AUTH_JWT_SECRET:?error Run python -m installer to generate secrets}
      - AUTH_JWT_TTL=${AUTH_JWT_TTL:-24h}
      - AUTH_COOKIE_NAME=${AUTH_COOKIE_NAME:-aithena_auth}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
      - RATE_LIMIT_REQUESTS_PER_MINUTE=${RATE_LIMIT_REQUESTS_PER_MINUTE:-100}
      - ZOOKEEPER_HOSTS=zoo1:2181,zoo2:2181,zoo3:2181
      - COLLECTIONS_DB_PATH=${COLLECTIONS_DB_PATH:-/data/collections/collections.db}
      - SOLR_AUTH_USER=${SOLR_READONLY_USER:-solr_readonly}
      - SOLR_AUTH_PASS=${SOLR_READONLY_PASS:-SolrReadOnly_dev2024!}
    volumes:
      - document-data:/data/documents
      - collections-db:/data/collections
      - type: bind
        source: ${AUTH_DB_DIR:?AUTH_DB_DIR is not set. Run 'python3 -m installer' to generate auth configuration}
        target: /data/auth
    networks:
      - default
  redis-commander:
    image: rediscommander/redis-commander:latest
    expose:
      - "8081"
    restart: unless-stopped
    stop_grace_period: 10s
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"const http = require('http'); const req = http.get({host: 'localhost', port: 8081, path: '/admin/redis/', timeout: 5000}, (res) => { process.exit(res.statusCode >= 200 && res.statusCode < 500 ? 0 : 1); }); req.on('error', () => process.exit(1)); req.on('timeout', () => { req.destroy(); process.exit(1); });\"",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - URL_PREFIX=/admin/redis
    networks:
      - default
  aithena-ui:
    build:
      context: ./src/aithena-ui
      args:
        VERSION: ${VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    expose:
      - "8080"
    restart: unless-stopped
    stop_grace_period: 10s
    depends_on:
      solr-search:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - default
  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    stop_grace_period: 10s
    depends_on:
      aithena-ui:
        condition: service_healthy
      solr-search:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      solr:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://127.0.0.1:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "80:80"
    volumes:
      - ./src/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./src/nginx/html:/usr/share/nginx/html:ro
      - document-data:/data/documents:ro
  zoo1:
    image: zookeeper:3.9
    entrypoint: /entrypoint-sasl.sh
    command: ["zkServer.sh", "start-foreground"]
    expose:
      - "2181"
    networks:
      - default
    environment:
      ZOO_4LW_COMMANDS_WHITELIST: "mntr,conf,ruok"
      ZOO_CFG_EXTRA: "admin.enableServer=false quorum.auth.enableSasl=true quorum.auth.learnerRequireSasl=true quorum.auth.serverRequireSasl=true authProvider.1=org.apache.zookeeper.server.auth.SASLAuthenticationProvider"
      ZOO_MY_ID: 1
      ZOO_SERVERS: server.1=zoo1:2888:3888;2181 server.2=zoo2:2888:3888;2181 server.3=zoo3:2888:3888;2181
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SERVER_JVMFLAGS: "-Djava.security.auth.login.config=/conf/jaas.conf"
    volumes:
      - zoo-data1_logs:/logs
      - zoo-data1_data:/data
      - zoo-data1_datalog:/datalog
      - zoo-backup:/backup
      - ./src/zookeeper/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    healthcheck:
      # ruok is the canonical ZK liveness check; the previous mntr-based
      # leader/follower test caused broken-pipe noise because grep exits
      # before nc finishes writing the large mntr response.  Solr's own
      # ZK client handles quorum detection, so ruok is sufficient here.
      test: ["CMD-SHELL", "printf ruok | nc -w 2 localhost 2181 | grep -q imok"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    stop_grace_period: 60s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
  zoo2:
    image: zookeeper:3.9
    entrypoint: /entrypoint-sasl.sh
    command: ["zkServer.sh", "start-foreground"]
    expose:
      - "2181"
    networks:
      - default
    environment:
      ZOO_4LW_COMMANDS_WHITELIST: "mntr,conf,ruok"
      ZOO_CFG_EXTRA: "admin.enableServer=false quorum.auth.enableSasl=true quorum.auth.learnerRequireSasl=true quorum.auth.serverRequireSasl=true authProvider.1=org.apache.zookeeper.server.auth.SASLAuthenticationProvider"
      ZOO_MY_ID: 2
      ZOO_SERVERS: server.1=zoo1:2888:3888;2181 server.2=zoo2:2888:3888;2181 server.3=zoo3:2888:3888;2181
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SERVER_JVMFLAGS: "-Djava.security.auth.login.config=/conf/jaas.conf"
    volumes:
      - zoo-data2_logs:/logs
      - zoo-data2_data:/data
      - zoo-data2_datalog:/datalog
      - zoo-backup:/backup
      - ./src/zookeeper/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "printf ruok | nc -w 2 localhost 2181 | grep -q imok"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    stop_grace_period: 60s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
  zoo3:
    image: zookeeper:3.9
    entrypoint: /entrypoint-sasl.sh
    command: ["zkServer.sh", "start-foreground"]
    expose:
      - "2181"
    networks:
      - default
    environment:
      ZOO_4LW_COMMANDS_WHITELIST: "mntr,conf,ruok"
      ZOO_CFG_EXTRA: "admin.enableServer=false quorum.auth.enableSasl=true quorum.auth.learnerRequireSasl=true quorum.auth.serverRequireSasl=true authProvider.1=org.apache.zookeeper.server.auth.SASLAuthenticationProvider"
      ZOO_MY_ID: 3
      ZOO_SERVERS: server.1=zoo1:2888:3888;2181 server.2=zoo2:2888:3888;2181 server.3=zoo3:2888:3888;2181
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SERVER_JVMFLAGS: "-Djava.security.auth.login.config=/conf/jaas.conf"
    volumes:
      - zoo-data3_logs:/logs
      - zoo-data3_data:/data
      - zoo-data3_datalog:/datalog
      - zoo-backup:/backup
      - ./src/zookeeper/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "printf ruok | nc -w 2 localhost 2181 | grep -q imok"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    stop_grace_period: 60s
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
  solr:
    image: solr:9.7
    # build:
    #   context: .
    #   dockerfile: ./src/solr/Dockerfile
    entrypoint: /entrypoint-sasl.sh
    command: ["solr-foreground"]
    restart: unless-stopped
    stop_grace_period: 60s
    volumes:
      - solr-data:/var/solr/data
      - document-data:/data/documents:ro
      - ./src/solr/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    environment:
      SOLR_MODULES: extraction,langid
      SOLR_SECURITY_MANAGER_ENABLED: "false"
      ZK_HOST: "zoo1:2181,zoo2:2181,zoo3:2181"
      SOLR_AUTH_USER: ${SOLR_ADMIN_USER:-solr_admin}
      SOLR_AUTH_PASS: ${SOLR_ADMIN_PASS:-SolrAdmin_dev2024!}
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SOLR_ZK_CREDS_AND_ACLS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
      SOLR_OPTS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
    expose:
      - "8983"
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -u $${SOLR_AUTH_USER}:$${SOLR_AUTH_PASS} http://localhost:8983/solr/admin/info/system || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 15
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1g
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    # command: solr-precreate -c books
    depends_on:
      zoo1:
        condition: service_healthy
      zoo2:
        condition: service_healthy
      zoo3:
        condition: service_healthy
  # second solr node
  solr2:
    image: solr:9.7
    # build:
    #   context: .
    #   dockerfile: ./src/solr/Dockerfile
    entrypoint: /entrypoint-sasl.sh
    command: ["solr-foreground"]
    restart: unless-stopped
    stop_grace_period: 60s
    volumes:
      - solr-data2:/var/solr/data
      - document-data:/data/documents:ro
      - ./src/solr/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    environment:
      SOLR_MODULES: extraction,langid
      SOLR_SECURITY_MANAGER_ENABLED: "false"
      ZK_HOST: "zoo1:2181,zoo2:2181,zoo3:2181"
      SOLR_AUTH_USER: ${SOLR_ADMIN_USER:-solr_admin}
      SOLR_AUTH_PASS: ${SOLR_ADMIN_PASS:-SolrAdmin_dev2024!}
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SOLR_ZK_CREDS_AND_ACLS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
      SOLR_OPTS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
    expose:
      - "8983"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -u $${SOLR_AUTH_USER}:$${SOLR_AUTH_PASS} http://localhost:8983/solr/admin/info/system || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 15
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1g
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    #command: solr-create -c books
    depends_on:
      zoo1:
        condition: service_healthy
      zoo2:
        condition: service_healthy
      zoo3:
        condition: service_healthy
      solr:
        condition: service_healthy
  solr3:
    image: solr:9.7
    # build:
    #   context: .
    #   dockerfile: ./src/solr/Dockerfile
    entrypoint: /entrypoint-sasl.sh
    command: ["solr-foreground"]
    restart: unless-stopped
    stop_grace_period: 60s
    volumes:
      - solr-data3:/var/solr/data
      - document-data:/data/documents:ro
      - ./src/solr/entrypoint-sasl.sh:/entrypoint-sasl.sh:ro
    environment:
      SOLR_MODULES: extraction,langid
      SOLR_SECURITY_MANAGER_ENABLED: "false"
      ZK_HOST: "zoo1:2181,zoo2:2181,zoo3:2181"
      SOLR_AUTH_USER: ${SOLR_ADMIN_USER:-solr_admin}
      SOLR_AUTH_PASS: ${SOLR_ADMIN_PASS:-SolrAdmin_dev2024!}
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SOLR_ZK_CREDS_AND_ACLS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
      SOLR_OPTS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
    expose:
      - "8983"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -u $${SOLR_AUTH_USER}:$${SOLR_AUTH_PASS} http://localhost:8983/solr/admin/info/system || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 15
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 1g
          cpus: "1.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    #command: solr-create -c books
    depends_on:
      zoo1:
        condition: service_healthy
      zoo2:
        condition: service_healthy
      zoo3:
        condition: service_healthy
      solr2:
        condition: service_healthy
  solr-init:
    image: solr:9.7
    depends_on:
      solr:
        condition: service_healthy
      solr2:
        condition: service_healthy
      solr3:
        condition: service_healthy
    environment:
      SOLR_SECURITY_MANAGER_ENABLED: "false"
      ZK_SASL_USER: ${ZK_SASL_USER:-solr}
      ZK_SASL_PASS: ${ZK_SASL_PASS:-SolrZkPass_dev}
      SOLR_ZK_CREDS_AND_ACLS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
      SOLR_OPTS: "-DzkCredentialsProvider=org.apache.solr.common.cloud.DigestZkCredentialsProvider -DzkACLProvider=org.apache.solr.common.cloud.DigestZkACLProvider -DzkDigestUsername=${ZK_SASL_USER:-solr} -DzkDigestPassword=${ZK_SASL_PASS:-SolrZkPass_dev}"
    volumes:
      - ./src/solr/books:/configsets/books:ro
      - ./src/solr/add-conf-overlay.sh:/scripts/add-conf-overlay.sh:ro
      - ./src/solr/security.json:/scripts/security.json:ro
    entrypoint:
      - /bin/bash
      - -ceu
      - |
          set -o pipefail

          ZK_HOST="zoo1:2181,zoo2:2181,zoo3:2181"
          SOLR_URL="http://solr:8983"

          until curl -fsS -u "$$SOLR_ADMIN_USER:$$SOLR_ADMIN_PASS" "$$SOLR_URL/solr/admin/info/system" >/dev/null 2>&1 || curl -fsS "$$SOLR_URL/solr/admin/info/system" >/dev/null 2>&1; do
            sleep 2
          done

          until [ "$$(curl -fsS "$$SOLR_URL/solr/admin/collections?action=CLUSTERSTATUS&wt=json" | grep -o '"[^"]*:8983_solr"' | sort -u | wc -l | tr -d '[:space:]')" -ge 3 ]; do
            sleep 2
          done

          # --- Security: Bootstrap BasicAuth + RBAC ---
          echo "Uploading security.json to ZooKeeper..."
          solr zk cp file:/scripts/security.json zk:/security.json -z "$$ZK_HOST"

          echo "Creating Solr users via Authentication API..."
          curl -fsS -u "$$SOLR_ADMIN_USER:$$SOLR_ADMIN_PASS" "$$SOLR_URL/solr/admin/authentication" \
            -H 'Content-Type: application/json' \
            -d '{"set-user": {"'"$$SOLR_ADMIN_USER"'": "'"$$SOLR_ADMIN_PASS"'", "'"$$SOLR_READONLY_USER"'": "'"$$SOLR_READONLY_PASS"'"}}' || \
          curl -fsS "$$SOLR_URL/solr/admin/authentication" \
            -H 'Content-Type: application/json' \
            -d '{"set-user": {"'"$$SOLR_ADMIN_USER"'": "'"$$SOLR_ADMIN_PASS"'", "'"$$SOLR_READONLY_USER"'": "'"$$SOLR_READONLY_PASS"'"}}'

          echo "Assigning roles via Authorization API..."
          curl -fsS -u "$$SOLR_ADMIN_USER:$$SOLR_ADMIN_PASS" "$$SOLR_URL/solr/admin/authorization" \
            -H 'Content-Type: application/json' \
            -d '{"set-user-role": {"'"$$SOLR_ADMIN_USER"'": "admin", "'"$$SOLR_READONLY_USER"'": "readonly"}}' || \
          curl -fsS "$$SOLR_URL/solr/admin/authorization" \
            -H 'Content-Type: application/json' \
            -d '{"set-user-role": {"'"$$SOLR_ADMIN_USER"'": "admin", "'"$$SOLR_READONLY_USER"'": "readonly"}}'

          echo "Enabling blockUnknown..."
          curl -fsS -u "$$SOLR_ADMIN_USER:$$SOLR_ADMIN_PASS" "$$SOLR_URL/solr/admin/authentication" \
            -H 'Content-Type: application/json' \
            -d '{"set-property": {"blockUnknown": true}}'

          echo "Security bootstrap complete."

          # --- books collection (multilingual-e5-base, 768D) ---
          if ! solr zk ls /configs -z "$$ZK_HOST" | grep -qx 'books'; then
            solr zk upconfig -z "$$ZK_HOST" -n books -d /configsets/books
          fi

          if ! curl -fsS -u "$$SOLR_AUTH_USER:$$SOLR_AUTH_PASS" "$$SOLR_URL/solr/admin/collections?action=LIST&wt=json" | grep -q '"books"'; then
            curl -fsS -u "$$SOLR_AUTH_USER:$$SOLR_AUTH_PASS" "$$SOLR_URL/solr/admin/collections?action=CREATE&name=books&collection.configName=books&numShards=1&replicationFactor=3&waitForFinalState=true&wt=json"
          fi

          SOLR_BASE_URL="$$SOLR_URL" SOLR_COLLECTION="books" SOLR_AUTH_USER="$$SOLR_AUTH_USER" SOLR_AUTH_PASS="$$SOLR_AUTH_PASS" sh /scripts/add-conf-overlay.sh

          echo "Solr init complete"
    networks:
      - default
    restart: "no"
volumes:
  rabbitmq-data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/rabbitmq-data"

  redis-data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/redis"
  document-data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "${BOOKS_PATH:-/data/booklibrary}"
  collections-db:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "${COLLECTIONS_DB_DIR:-/source/volumes/collections-db}"
  solr-data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/solr-data"
  solr-data2:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/solr-data2"
  solr-data3:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/solr-data3"
  zoo-data1_logs:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data1/logs"
  zoo-data1_data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data1/data"
  zoo-data1_datalog:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data1/datalog"
  zoo-data2_logs:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data2/logs"
  zoo-data2_data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data2/data"
  zoo-data2_datalog:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data2/datalog"
  zoo-data3_logs:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data3/logs"
  zoo-data3_data:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data3/data"
  zoo-data3_datalog:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-data3/datalog"
  zoo-backup:
    driver: local
    driver_opts:
      type: "none"
      o: "bind"
      device: "/source/volumes/zoo-backup"
networks:
  default:
