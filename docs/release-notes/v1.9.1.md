# Aithena v1.9.1 Release Notes — Docker Build Fix

_Date:_ 2026-03-20  
_Prepared by:_ Newt (Product Manager)

## Summary

Patch release that fixes the aithena-ui Docker build failure caused by a missing `.npmrc` file in the Dockerfile build context. This ensures the frontend builds correctly with the `legacy-peer-deps=true` configuration required for ESLint 10 compatibility.

## Issues Resolved

| # | Title | Type |
|---|-------|------|
| [#616](https://github.com/jmservera/aithena/issues/616) | aithena-ui Docker build fails — .npmrc not copied before npm ci | Bug |

## Changes

### Fixed

- **aithena-ui Dockerfile**: Copy `.npmrc` alongside `package*.json` before running `npm ci` so that the `legacy-peer-deps=true` setting is respected. Without this, `npm ci` fails with an ERESOLVE error because `eslint-plugin-jsx-a11y@6.10.2` has a peer dependency on `eslint ^3-^9` while the project uses `eslint@10.0.3`.

## Merged pull requests

No pull request metadata was available in the validation context for v1.9.1.

## Breaking changes

**None.**

All changes in v1.9.1 are backward-compatible Docker build improvements with no breaking changes to APIs, configuration, or user workflows.

## Known issues

**None.**

All reported issues for v1.9.1 have been resolved.

## Upgrade instructions

### For users running Aithena v1.9.0

1. Update to the v1.9.1 release:
   ```bash
   git fetch origin
   git checkout v1.9.1
   ```

2. Verify the fix:
   - Frontend Docker image builds successfully without ERESOLVE errors
   - Application starts cleanly with no missing dependencies

3. No new environment variables, configuration changes, or database migrations are required.

### For Docker Compose deployments

If deploying via Docker Compose:

```bash
docker compose pull
docker compose up -d
```

The build fix is automatically applied upon rebuild.

## Validation checklist

Before using v1.9.1 in production, verify:

- [ ] Frontend Docker image builds successfully: `docker compose build aithena-ui`
- [ ] All services start cleanly: `docker compose up`
- [ ] Application loads and responds normally
- [ ] No console errors in browser DevTools
- [ ] Version display shows v1.9.1

## Deprecations

**None.**

## Next steps

- Continue with v1.10.0 development cycle
- Monitor for any additional build or deployment issues

---

**For support, documentation, and deployment guides, see:**
- User Manual: `docs/user-manual.md`
- Admin Manual: `docs/admin-manual.md`
- Quickstart: `docs/quickstart.md`
- Design System: `docs/design-system.md`
