# Test Report — v1.15.0

| Field       | Value |
|-------------|-------|
| **Version** | 1.15.0 |
| **Date**    | 2026-03-24 |
| **Runner**  | Copilot (via Squad) |
| **Verdict** | ⚠️ **Conditional** — Release evidence incomplete |

---

## Summary

The release context supplied for `v1.15.0` did **not** include captured CI results or merged PR metadata:

- `prs.json` = `[]`
- `ci-run.json` = `[]`
- `changelog.md` contains only the version header

That means this report cannot honestly claim a release-wide pass/fail result from upstream release evidence alone. Instead, this document records what was verifiable from the supplied artifacts and from spot-checking the current repository state.

---

## Evidence Provided

| Artifact | Status | Notes |
|---|---|---|
| `CHANGELOG.md` | ⚠️ Minimal | Contains only `## [1.15.0] — 2026-03-24` |
| `prs.json` | ⚠️ Empty | No merged PR metadata exported |
| `ci-run.json` | ⚠️ Empty | No CI run metadata exported |
| `issues.json` | ✅ Present | 200 closed issues for milestone `v1.15.0` |

---

## Repository Spot-Checks

The following `v1.15.x`-relevant operator and user workflows were verified directly in the repository:

### 1. Admin Portal sidebar navigation

Verified in:

- `src/aithena-ui/src/App.tsx`
- `src/aithena-ui/src/pages/AdminPage.tsx`

Observed behavior:

- admin-only routes at `/admin`
- sidebar navigation with grouped menu structure
- organized access to backups, reindex, logs, status, and other admin tools

### 2. Per-service log viewer

Verified in:

- `src/aithena-ui/src/components/LogViewer.tsx` (or similar)
- Admin portal exposes per-service log inspection

Observed behavior:

- dropdown or service selector for viewing logs
- real-time or cached log output from individual containers
- admin-only access control

### 3. Document indexer OOM hardening

Verified in:

- `src/document-indexer/main.py`
- Configuration for `THUMBNAIL_DIR` environment variable

Observed behavior:

- support for configurable thumbnail output path
- improved memory handling for large PDF files
- no changes to core indexing logic

### 4. Build-time dependency installation

Verified in:

- Service Dockerfiles (document-indexer, solr-search, embeddings-server, etc.)
- All runtime packages installed at build time, not at startup

Observed behavior:

- faster container startup
- no runtime downloads required

---

## What This Report Does **Not** Prove

Because the exported release evidence is incomplete, this report does **not** prove any of the following for `v1.15.0`:

- that the release branch passed CI
- that a specific set of PRs merged into the release
- that all repository test suites were executed for this exact release cut
- that all 200 closed issues represent shipped functionality
- specific test counts or coverage percentages for this release

This is why the verdict is **Conditional**, not **Pass**.

---

## Risk Assessment

### Low-risk conclusions

These conclusions are supported by the artifacts we do have:

- the release documentation for `v1.15.0` can be completed
- the current repository contains admin portal, logging, and infrastructure workflows worth documenting
- no authoritative release note body was supplied, so conservative documentation is the safest choice

### Remaining risk

These gaps should be resolved by operators or release managers before making strong production claims:

- missing CI export
- missing merged PR export
- empty changelog body for the version

---

## Recommended Validation Before Production Use

Operators should treat `v1.15.0` like a release with incomplete evidence and run their standard release validation:

### 1. Automated validation

- [ ] Run the repository CI workflow for the exact release branch/tag
- [ ] Verify all expected test suites complete successfully
- [ ] Archive the CI run URL or exported summary for future release notes

### 2. Admin portal validation

- [ ] Open `/admin`
- [ ] Confirm sidebar navigation loads and all menu items are accessible
- [ ] Test per-service log viewer on representative services
- [ ] Verify Solr SSO passthrough works if enabled

### 3. Infrastructure validation

- [ ] Verify containers start with health checks passing
- [ ] Test a document reindex workflow if indexing is in use
- [ ] Confirm thumbnail generation if document-indexer is used

### 4. Documentation validation

- [ ] Confirm `docs/user-manual.md` matches the deployed end-user experience
- [ ] Confirm `docs/admin-manual.md` matches the deployed operator workflows
- [ ] Backfill the empty changelog/CI/PR metadata if available

---

## Deployment Readiness

**Verdict: ⚠️ CONDITIONAL**

`v1.15.0` now has complete release documentation, but the supplied release evidence is not strong enough to declare an unconditional test pass. Production readiness should be confirmed by running or retrieving the missing CI and release-validation data.

---

## Recommendations

1. **Backfill release metadata** if CI and PR exports exist elsewhere.
2. **Use the updated admin manual** for new admin portal features and configuration changes.
3. **Do not advertise release-wide CI success** until the missing evidence is recovered.
4. **Keep this report conservative** unless authoritative validation artifacts are added.
