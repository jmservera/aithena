# Test Report — v1.17.1

| Field       | Value |
|-------------|-------|
| **Version** | 1.17.1 |
| **Date**    | 2026-03-28 |
| **Runner**  | Copilot (via Squad) |
| **Verdict** | ⚠️ **Conditional** — Release evidence incomplete; security patch scope is narrow |

---

## Summary

The release context supplied for `v1.17.1` did **not** include captured CI results or merged PR metadata:

- `prs.json` = `[]` (empty)
- `ci-run.json` = `[]` (empty)
- `changelog.md` contains only the version header

That means this report cannot claim a release-wide pass/fail result from upstream release evidence alone. However, `v1.17.1` is a targeted security patch with minimal scope: a single infrastructure fix (#1237) to harden GitHub Actions secrets handling. Given the narrow scope (CI/CD only, no code changes to user-facing services), the risk profile is low.

---

## Evidence Provided

| Artifact | Status | Notes |
|---|---|---|
| `CHANGELOG.md` | ⚠️ Minimal | Contains only `## [1.17.1] — 2026-03-28` |
| `prs.json` | ⚠️ Empty | No merged PRs provided |
| `ci-run.json` | ⚠️ Empty | No CI run metadata exported |
| `issues.json` | ✅ Present | 200 closed issues (milestone backlog + 1 new security fix #1237) |

---

## Repository Spot-Checks

The following areas were identified from the v1.17.1 scope (security hardening only).

### 1. CI Secrets Hardening — GitHub Actions Environment (#1237)

**Expected behavior after fix:**

- `.github/workflows/build-containers.yml` includes a top-level `environment: build` directive
- Secrets (`GHCR_TOKEN`, `GHCR_USERNAME`) are referenced in the workflow and passed to jobs
- GitHub Actions environment named `build` is configured with protection rules
- Workflows running outside the protected environment cannot access the secrets
- Code scanning alert #230 (zizmor) is resolved in GitHub

**Where to verify in the repository:**

- `.github/workflows/build-containers.yml` — check for `environment: build` directive at workflow or job level
- GitHub repository settings → Environments — verify `build` environment exists with protection rules
- GitHub code scanning dashboard — alert #230 should show as resolved

**Risk if unfixed:** Secrets could be accidentally exposed in fork scenarios or unprotected workflow runs; code scanning alert remains open.

---

## What This Report Does **Not** Prove

Because the exported release evidence is incomplete, this report does **not** prove any of the following for `v1.17.1`:

- that the release branch passed CI
- that all code builds successfully
- that all repository test suites were executed for this exact release cut
- specific test counts or coverage percentages for this release
- that v1.17.0 functionality (GPU acceleration, search, etc.) continues to work

This is why the verdict is **Conditional**, not **Pass**.

---

## Risk Assessment

### Low-risk conclusions

- v1.17.1 is a **security patch only** (1 targeted issue)
- No code changes to user-facing services (aithena-ui, solr-search, document-indexer, embeddings-server)
- No breaking API, deployment, or configuration changes
- No dependency updates in this release
- Change is infrastructure-only (CI/CD) — does not affect runtime behavior
- Backward compatible with v1.17.0 (CPU-only and GPU-enabled deployments unaffected)

### Remaining risk

These gaps should be resolved by operators or release managers before making strong production claims:

- Missing CI export — cannot confirm the exact build status for this release
- Empty PR export — cannot confirm which code was included (if any)
- Empty changelog body — no authoritative summary from the release team
- No independent verification that v1.17.0 GPU features still work in v1.17.1

---

## Recommended Validation Before Production Use

### 1. Automated validation

- [ ] Run the repository CI workflow for the exact release branch/tag
- [ ] Verify all expected test suites complete successfully (lint, unit, integration)
- [ ] Archive the CI run URL or exported summary for future release notes

### 2. Security validation (specific to this patch)

- [ ] Verify GitHub Actions environment `build` exists and is configured with protection rules
- [ ] Verify `.github/workflows/build-containers.yml` includes `environment: build`
- [ ] Confirm GitHub code scanning alert #230 is resolved
- [ ] (Optional) Run `zizmor` locally on the workflow file to verify it passes

### 3. Backward compatibility validation

- [ ] Verify `docker compose up -d` (CPU mode) starts successfully
- [ ] Run a keyword search and confirm results are returned
- [ ] Run a semantic search and confirm results are returned
- [ ] (If GPU enabled) Verify `docker compose --profile nvidia up -d` or `--profile intel up -d` still works

### 4. v1.17.0 GPU feature regression check

- [ ] (If GPU available) Build embeddings-server image and verify GPU acceleration still works
- [ ] Test `DEVICE=cuda` with NVIDIA GPU (if available) — verify CUDA is detected
- [ ] Test `DEVICE=auto` with no GPU — verify automatic fallback to CPU
- [ ] Verify no new errors or warnings in container logs

---

## Deployment Readiness

**Verdict: ⚠️ CONDITIONAL**

`v1.17.1` is a security patch with minimal, targeted scope (1 infrastructure fix). The supplied release evidence is not strong enough to declare an unconditional test pass due to missing CI and PR metadata. However, the risk profile for this patch is low because:

1. No code changes to runtime services
2. No breaking changes
3. No dependency updates
4. Complete backward compatibility with v1.17.0

Production readiness should be confirmed by:
- Running or retrieving the missing CI validation data
- Completing the manual validation checklist above
- Confirming GPU features from v1.17.0 still work

---

## Recommendations

1. **Retrieve missing CI metadata** — obtain automated test results for the release branch to confirm the build passed.
2. **Validate security patch** — confirm GitHub Actions environment `build` is correctly configured and alert #230 is resolved.
3. **Test backward compatibility** — verify CPU-only and GPU-enabled deployments from v1.17.0 work without modification on v1.17.1.
4. **Validate GPU regression** — if GPU acceleration is used, run a quick functional test (embeddings, semantic search) to confirm no regression from v1.17.0.
5. **Do not advertise release-wide CI success** until the missing CI evidence is recovered.
