# Test Report — v1.11.0

| Field       | Value                     |
|-------------|---------------------------|
| **Version** | 1.11.0                    |
| **Date**    | 2026-03-22                |
| **Runner**  | Lambert (Tester)          |
| **Verdict** | ⚠️ **Conditional** — Requires operator validation |

---

## Summary

v1.11.0 is a test coverage expansion release addressing the **Search Results Redesign** PRD (docs/prd/search-results-redesign.md). The release merged **18 PRs** covering **27 closed issues** focused on increasing test coverage across three waves of development.

**Evidence provided:**
- ✅ 18 merged PRs (PRs #798, #832-850)
- ✅ 27 closed issues (#796-831)
- ❌ CI execution logs empty (`ci-run.json` contains `[]`)

**CI status:** No release-wide CI test run was provided. Operators must execute all test suites before deployment.

---

## Shipped Work

### Search Results Redesign — Test Coverage Waves

The release is organized into three waves of test coverage expansion:

**Wave 1 Coverage** (Issues #807-817)
- PR #839 ships 38 new tests across solr-search, document-indexer, aithena-ui
- Focused on: folder facet edge cases, thumbnail generation, BookCard UI, chunk display

**Wave 2 Coverage** (Issues #819-823)
- PR #845 ships 17 new tests across solr-search, aithena-ui
- Focused on: circuit breaker, similar books, collection badges, BookDetailView

**Wave 3 Coverage** (Issues #825-829)
- PR #850 ships 12 new tests across document-indexer, solr-search, aithena-ui
- Focused on: PDF thumbnails, book detail response, thumbnail error states

**Additional PRs with test counts:**
- PR #840: 11 new tests
- PR #841: 21 tests mentioned
- PR #843: 13 new tests
- PR #844: 7 new tests
- PR #847: 12 new tests
- PR #848: 6 tests mentioned
- PR #849: 3 tests mentioned

**Cross-cutting PRs:**
- PR #798, #832-838, #842, #846: Supporting work (configuration, documentation, refactoring)

---

## Test Suite Inventory

Per Lambert's history (v1.10.0 baseline), the project has **690 tests** across 6 services. The v1.11.0 PRs add coverage to this baseline.

### Baseline Test Commands (from v1.10.0)

| Service | Command | Notes |
|---------|---------|-------|
| **solr-search** | `cd src/solr-search && uv run pytest -v --tb=short` | ~219 tests (v1.10.0 baseline) |
| **aithena-ui** | `cd src/aithena-ui && npm test` | ~189 tests (v1.10.0 baseline), uses Vitest + React Testing Library |
| **document-indexer** | `cd src/document-indexer && uv run pytest -v --tb=short` | 91 + 4 skipped (v1.10.0 baseline) |
| **admin** | `cd src/admin && uv run pytest -v --tb=short` | 81 tests (v1.10.0 baseline) |
| **document-lister** | `cd src/document-lister && uv run pytest -v --tb=short` | 12 tests (v1.10.0 baseline) |
| **embeddings-server** | `cd src/embeddings-server && uv run pytest -v --tb=short` | 9 tests (v1.10.0 baseline), requires manual `pip install pytest httpx` |

### Lint Commands

```bash
cd src/solr-search && uv run ruff check .
cd src/document-indexer && uv run ruff check .
cd src/aithena-ui && npm run lint
cd src/aithena-ui && npm run format:check
```

### Coverage Thresholds

- solr-search: ≥90%
- document-indexer: ≥80%

---

## Evidence Status

| Evidence                     | Status | Notes |
|------------------------------|--------|-------|
| Merged PRs                   | ✅ Verified | 18 PRs (#798, #832-850) |
| Closed issues                | ✅ Verified | 27 issues (#796-831) |
| v1.11.0 label                | ❌ Not found | Issues do not have v1.11.0 label in artifact |
| CHANGELOG.md entry           | ❌ Not found | No [1.11.0] section in changelog artifact |
| CI execution logs            | ❌ Empty | `ci-run.json` is `[]` |
| Test pass/fail counts        | ❌ Cannot verify | No CI logs provided |
| Coverage percentages         | ❌ Cannot verify | No coverage report in artifact |

---

## Operator Validation Checklist

Before deploying v1.11.0, operators must:

### 1. Run All Test Suites

Execute the baseline test commands from the inventory above. **All suites must pass.**

```bash
# Core services
cd src/solr-search && uv run pytest -v --tb=short
cd src/aithena-ui && npm test
cd src/document-indexer && uv run pytest -v --tb=short
cd src/admin && uv run pytest -v --tb=short
cd src/document-lister && uv run pytest -v --tb=short
cd src/embeddings-server && uv run pytest -v --tb=short

# Linting
cd src/solr-search && uv run ruff check .
cd src/aithena-ui && npm run lint && npm run format:check
```

### 2. Verify Coverage Thresholds

Check that coverage meets minimum thresholds:
- solr-search: ≥90%
- document-indexer: ≥80%

### 3. Spot-Check New Test Areas

Based on PR descriptions, verify tests exist for:
- Folder facet edge cases (empty paths, deep nesting)
- PDF thumbnail generation (landscape/portrait aspect ratios)
- BookCard UI rendering (highlights, collection badges)
- Chunk display in search results
- Circuit breaker error handling
- Similar books API integration
- BookDetailView focus management

### 4. Manual Smoke Test

- [ ] Application starts: `docker compose up -d`
- [ ] All services healthy
- [ ] Frontend loads without errors
- [ ] Search returns results
- [ ] PDF thumbnails display correctly
- [ ] Collection badges appear on search results
- [ ] Similar books panel works in book detail view

---

## Known Limitations

### CI Execution Status Unknown

`ci-run.json` is empty, so this report cannot confirm:
- Total test count after PRs merged
- Pass/fail status of any test suite
- Coverage percentages achieved
- Lint check results

**Consequence:** The verdict is **conditional** on operator test execution.

### No CHANGELOG Entry

The CHANGELOG artifact (`/tmp/release-context/changelog.md`) contains: `## No CHANGELOG entry for v1.11.0`

This means the release is **not documented** in the project changelog. Operators should verify whether this is intentional (e.g., internal development milestone) before deployment.

### No v1.11.0 Milestone Label

The 27 closed issues do not have a `v1.11.0` label in the provided artifact. They appear to be linked by issue number range (#796-831) and PR references rather than explicit milestone tracking.

---

## Verdict

**⚠️ Conditional** — Requires operator validation.

**Why conditional:**
- No CI test execution results provided (`ci-run.json` is empty)
- No CHANGELOG entry documenting the release
- No v1.11.0 milestone label found in issue artifact
- Cannot verify test counts, pass/fail status, or coverage percentages

**What operators must do:**
1. Execute all test suites using commands in the inventory
2. Verify all tests pass
3. Check coverage thresholds (solr-search ≥90%, document-indexer ≥80%)
4. Perform manual smoke test checklist
5. Determine if the missing CHANGELOG entry is acceptable for this release

**Do not deploy if:**
- Any test suite fails
- Coverage drops below thresholds
- Manual smoke test reveals regressions

---

**Report prepared by:** Lambert (Tester)  
**Date:** 2026-03-22  
**Status:** Evidence-only report — operator validation required before deployment
