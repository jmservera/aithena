# Test Report — v1.12.2

| Field | Value |
|---|---|
| **Version** | 1.12.2 |
| **Date** | 2026-03-24 |
| **Runner** | Lambert (Tester) |
| **Verdict** | ⚠️ **Conditional** — operator validation required |

---

## Summary

The provided release context supports a **targeted hotfix** interpretation for Aithena v1.12.2.

The only release-specific change explicitly identified in the supplied artifacts is **Issue #949**, which states that the v1.12.1 `embeddings-server` image omitted `model_utils.py`, causing startup failure and breaking **semantic** and **hybrid** search. Keyword search is reported as unaffected.

The release bundle does **not** include CI execution logs or a merged PR list, so this report documents what can be verified from the artifacts and defines the validation operators must complete before deployment.

### Evidence provided

- ✅ `changelog.md` confirms the version/date header: `## [1.12.2] — 2026-03-24`
- ✅ `issues.json` includes Issue `#949`, which explicitly calls for a `v1.12.2` hotfix release
- ❌ `prs.json` is empty
- ❌ `ci-run.json` is empty

---

## Release-Specific Validation Target

### Issue #949 — HOTFIX: embeddings-server fails to start

**Failure described in issue context**

- `embeddings-server` crashes at startup with `ModuleNotFoundError: No module named 'model_utils'`
- semantic search is unavailable
- hybrid search is unavailable
- keyword search still works

**What v1.12.2 must prove**

- the embeddings image contains `model_utils.py`
- `embeddings-server` starts successfully
- semantic search works again
- hybrid search works again
- the hotfix does not regress keyword search or previously shipped v1.12.1 behavior

**Note on PR evidence**

Issue #949 references **PR #948** as the implementation vehicle, but that PR is not present in the provided `prs.json`. Treat the PR reference as issue-derived context only, not independently verified release metadata.

---

## Expected Test Coverage

Because no CI execution record was provided, the following checks are the minimum release-gating validation for v1.12.2.

### 1. Hotfix-specific checks

- [ ] Verify `embeddings-server` starts without import errors
- [ ] Verify no `ModuleNotFoundError: No module named 'model_utils'` appears in logs
- [ ] Verify embeddings health endpoint responds successfully
- [ ] Verify one semantic search returns results
- [ ] Verify one hybrid search returns results
- [ ] Verify keyword search still returns results

### 2. Existing repository test suites

The repository already defines these relevant validation commands:

| Service | Command |
|---|---|
| `aithena-ui` | `cd src/aithena-ui && npm test` |
| `aithena-ui` lint | `cd src/aithena-ui && npm run lint && npm run format:check` |
| `solr-search` | `cd src/solr-search && uv run pytest -v --tb=short` |
| `solr-search` lint | `cd src/solr-search && uv run ruff check .` |
| `document-indexer` | `cd src/document-indexer && uv run pytest -v --tb=short` |
| `document-lister` | `cd src/document-lister && uv run pytest -v --tb=short` |
| `embeddings-server` | `cd src/embeddings-server && uv run pytest -v --tb=short` |
| `admin` | `cd src/admin && uv run pytest -v --tb=short` |

---

## CI / Automation Evidence

**Status:** Missing from the supplied release bundle

The provided CI artifact is:

```json
[]
```

Therefore this report cannot confirm:

- that CI ran for v1.12.2
- that automated tests passed for v1.12.2
- that lint/build steps passed for v1.12.2
- that release images were built successfully in CI for v1.12.2

This is the primary reason the final verdict remains conditional.

---

## Operator Validation Checklist

### 1. Run the existing automated suites

- [ ] `cd src/aithena-ui && npm test`
- [ ] `cd src/aithena-ui && npm run lint && npm run format:check`
- [ ] `cd src/solr-search && uv run pytest -v --tb=short`
- [ ] `cd src/solr-search && uv run ruff check .`
- [ ] `cd src/document-indexer && uv run pytest -v --tb=short`
- [ ] `cd src/document-lister && uv run pytest -v --tb=short`
- [ ] `cd src/embeddings-server && uv run pytest -v --tb=short`
- [ ] `cd src/admin && uv run pytest -v --tb=short`

### 2. Verify runtime recovery after deployment

- [ ] Start the stack: `docker compose up -d`
- [ ] Confirm services are up: `docker compose ps`
- [ ] Check embeddings health: `curl http://localhost:8085/health`
- [ ] Review logs for startup/import failures: `docker compose logs embeddings-server`

### 3. Verify search behavior

- [ ] Run a keyword search and confirm results are returned
- [ ] Run a semantic search and confirm results are returned
- [ ] Run a hybrid search and confirm results are returned
- [ ] Confirm no unexpected degradation or 5xx errors are shown to users

### 4. Spot-check v1.12.1 behavior still holds

- [ ] Collections still use real backend data
- [ ] Login form still shows **Remember me**
- [ ] Search result text previews remain truncated for readability
- [ ] Thumbnails still render for newly indexed documents

---

## Risk Assessment

### Supported by evidence

- v1.12.2 exists as a release target in the supplied changelog header
- Issue #949 explicitly defines the release as an embeddings startup hotfix
- the reported production impact is loss of semantic and hybrid search

### Not supported by evidence

- CI pass/fail status
- merged PR inventory for the tag
- successful post-fix deployment logs
- proof from the release bundle that the built image now contains `model_utils.py`

### Risk level

**Medium**

The functional scope appears narrow, but the operational impact is high if the hotfix fails because embeddings-backed search remains unavailable.

---

## Verdict

**⚠️ Conditional — approve only after operator validation**

Based on the provided artifacts, Aithena v1.12.2 is best documented as a focused hotfix release intended to restore `embeddings-server` startup and recover semantic/hybrid search after the v1.12.1 packaging regression described in Issue #949.

Because the release bundle contains **no CI evidence** and **no merged PR list**, this release should not be treated as fully validated until operators complete the checklist above and record the results.

---

## Recommended Sign-Off Statement

> Aithena v1.12.2 is documented as a focused hotfix for the `embeddings-server` startup failure described in Issue #949. The release purpose is supported by the supplied issue evidence, but CI and merged-PR artifacts were not provided. Approve deployment only after confirming embeddings health and successful semantic/hybrid search behavior in the target environment.

---

**Report prepared by:** Lambert (Tester)  
**Status:** Evidence-only report with required operator follow-up
