# Test Report — v2.5.0

| Field | Value |
|---|---|
| **Version** | 2.5.0 |
| **Date** | 2026-06-06T07:14:37.128+00:00 |
| **Runner** | Lambert (Tester) / Newt (Product Manager) |
| **Issue** | #1340 (Update E2E tests for Solr 10), #1341 (Update CI/CD for Solr 10 image builds), #1662 (Harden OpenVINO release gates) |
| **Verdict** | ⚠️ **Pre-release validation passed; direct CI run JSON not available.** All 13 v2.5.0 milestone issues are closed. Pre-release validation issues #1654, #1655, and #1656 are closed, and issue #1662 (OpenVINO dependency drift fix) was resolved with a successful pre-release re-run at commit `a8a5cb5` (GitHub Actions run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418)). Formal `ci-run.json` and `prs.json` data were not supplied in the release context; the pre-release workflow run provides the primary CI confirmation for this release. |

---

## Active Release Target

- `VERSION` milestone: `v2.5.0`
- Previous release tag: `v2.3.0` (Solr 9)
- Scope: Solr 9 → Solr 10 upgrade, single-node SolrCloud lightweight topology, Security UI, schema migration
- v2.5.0 milestone issues closed: 13 (#1335–#1359)
- Pre-release validation issues closed: 3 (#1654, #1655, #1656)
- OpenVINO pre-release hardening issue closed: 1 (#1662 — fix applied at commit `a8a5cb5`)
- Pre-release GitHub Actions run: ✅ [run 27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) (successful re-run after #1662 fix)
- CI run data provided: ❌ Not available (`ci-run.json` is empty)
- PR merge data provided: ❌ Not available (`prs.json` is empty)

---

## Summary

v2.5.0 is the Solr 10 upgrade release. The implemented evidence path covers:

1. Upgrade the Solr base image from Apache Solr 9 to Apache Solr 10 (#1335).
2. Migrate HNSW vector search schema parameters for Solr 10 compatibility (#1336).
3. Rewrite `solr-init` bootstrap using Solr 10 CLI APIs (#1337).
4. Explicitly handle the `blockUnknown` security default change in Solr 10 (#1338).
5. Verify `wt=json` response writer and `PathHierarchyTokenizer` under Solr 10 (#1339, #1352).
6. Validate the single-node SolrCloud lightweight topology; true ZooKeeper-free standalone/core mode remains unshipped in v2.5 (#1342).
7. Surface Solr 10's built-in Security UI via Admin → Security (#1350).
8. Update the CI/CD pipeline and E2E test suite for Solr 10 images (#1340, #1341).
9. Complete a security audit of Solr 10 defaults and publish the Solr 9→10 migration runbook (#1358, #1359).

No CI run JSON or PR merge data were provided in the release context (`ci-run.json: []`, `prs.json: []`). See the Known Gaps section.

---

## CI / Check Evidence

| Artifact | Result | Evidence |
|---|---:|---|
| Pre-release validation (run 27026253418) | ✅ Passed | Issue #1662 closed — initial pre-release at `8b2b46b` failed (OpenVINO version drift); fix applied at `a8a5cb5`; successful pre-release run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) passed. |
| Pre-release deprecation warnings (#1654) | ✅ Closed | Known deprecation warnings during v2.5.0 pre-release classified and allowlisted. |
| Pre-release config warnings (#1655) | ✅ Closed | Configuration warnings during v2.5.0 pre-release reviewed and accepted. |
| Pre-release validation failure (#1656) | ✅ Closed | Resolved by issue #1662 (OpenVINO fix + re-run). |
| CI run data (`ci-run.json`) | ⚠️ Not provided | Release context file is empty (`[]`). Pre-release run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) is the primary CI confirmation. |
| PR merge data (`prs.json`) | ⚠️ Not provided | Release context file is empty (`[]`). No PR-level CI confirmation is available in this report. |
| v2.5.0 milestone issues closed | ✅ 13/13 | All 13 `[v2.5]` milestone issues (#1335–#1359) are present and closed in the release context `issues.json`. |

**Required checks to confirm before final release tag:**

- `CI - Unit & Integration Tests` (all services: document-indexer, solr-search, aithena-ui, document-lister, embeddings-server; Python lint; Compose security regression) — confirmed passing in pre-release run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418)
- `Dev Integration Test (Single-Node)` (integration and E2E tests against Solr 10) — confirmed passing in pre-release run
- `Security Scanning` (Bandit, Checkov, zizmor, CodeQL) — confirmed passing in pre-release run

---

## Per-Service / Gate Breakdown

| Area | Status | Evidence |
|---|---:|---|
| Solr 10 base image upgrade | ✅ Closed | Issue #1335 closed — Solr base image upgraded to Apache Solr 10.x. |
| HNSW schema migration | ✅ Closed | Issue #1336 closed — `hnswMaxConnections`→`hnswM`, `hnswBeamWidth`→`hnswEfConstruction` in `managed-schema.xml`; Solr 9 compatibility rewrites remain in bootstrap. |
| solr-init CLI rewrite | ✅ Closed | Issue #1337 closed — solr-init uses Solr 10 CLI (`solr create`, `solr config`, `solr auth`); Solr 9 invocations removed. |
| blockUnknown compatibility posture | ✅ Closed | Issue #1338/#1663 closed — Aithena v2.5 explicitly keeps `blockUnknown=false` for health/metrics compatibility while authenticating container health checks. |
| wt=json response writer | ✅ Closed | Issue #1339 closed — Response writer verified; `solr-search` handles Solr 10 JSON responses correctly. |
| E2E test suite (Solr 10) | ✅ Closed | Issue #1340 closed — Playwright and integration test suites updated for Solr 10 API responses and UI. |
| CI/CD image builds (Solr 10) | ✅ Closed | Issue #1341 closed — GitHub Actions builds and publishes Solr 10 container images. |
| Lightweight Solr topology | ✅ Closed | Issue #1342 closed — single-node SolrCloud remains the shipped lightweight topology; true ZooKeeper-free standalone/core mode is not shipped in v2.5. |
| Security UI integration | ✅ Closed | Issue #1350 closed — Admin → Security page links to Solr 10 Security UI at `/admin/solr/ui/#/~security`. |
| PathHierarchyTokenizer | ✅ Closed | Issue #1352 closed — Folder facet tokenizer verified; behavior unchanged between Solr 9 and Solr 10. |
| Documentation (Solr 10) | ✅ Closed | Issue #1353 closed — Admin manual, user manual, deployment docs updated for Solr 10. |
| Security audit | ✅ Closed | Issue #1358 closed — Solr 10 default configuration security audit completed; findings in security posture docs. |
| Migration runbook | ✅ Closed | Issue #1359 closed — Solr 9→10 migration runbook published at `docs/migration/solr-10-production-runbook.md`. |
| document-indexer | ⚠️ No direct CI data | No v2.5.0 issues; pre-release run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) confirms passing. |
| document-lister | ⚠️ No direct CI data | No v2.5.0 issues; pre-release run confirms passing. |
| embeddings-server | ✅ Pre-release passed | Issue #1662 — initial OpenVINO version drift failure resolved at `a8a5cb5`; pre-release confirms passing. |
| aithena-ui | ⚠️ No direct CI data | Security UI scaffold is UI work (#1350); pre-release run confirms passing. |

---

## Critical Validations

- **Schema migration coverage:** Issue #1336 confirms HNSW parameter names were updated to Solr 10 nomenclature. The migration runbook (#1359) covers the upgrade path for existing Solr 9 collections.
- **blockUnknown explicit:** Issue #1338/#1663 confirms Aithena v2.5 explicitly keeps `blockUnknown=false`, preserving the current health/metrics access posture while authenticated health checks are used by the stack.
- **Lightweight topology:** Issue #1342 validation is represented as single-node SolrCloud in shipped docs/code; ZooKeeper-free standalone/core mode remains unshipped in v2.5.
- **Folder facets unaffected:** Issue #1352 confirms `PathHierarchyTokenizer` behavior is unchanged in Solr 10; folder path facets require no operator action.
- **Security UI scaffold:** Issue #1350 confirms the Admin → Security page links correctly to Solr 10's native Security UI. Aithena does not proxy or replicate Solr's security management — it provides navigational access only.
- **wt=json compatibility:** Issue #1339 confirms the `solr-search` service handles Solr 10 JSON response format without code changes.

---

## Known Gaps / Accepted Risks

- **No CI run JSON provided.** The `ci-run.json` release context file is empty. This report cannot confirm per-job CI results or per-service test pass counts from a structured data source. The pre-release workflow run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) at commit `a8a5cb5` is the primary CI confirmation for this release.
- **No PR merge data provided.** The `prs.json` context file is empty. PR-level CI evidence is not available in this report.
- **Pre-release initially failed (#1656).** The initial pre-release run at `dev` commit `8b2b46b` failed due to embeddings-server OpenVINO version drift (issue #1662). The fix (pinned OpenVINO 2025.4, added `openvino-tokenizers`, updated `optimum-intel` version range) was applied at `a8a5cb5` and the re-run passed.
- **Solr 10 migration runbook end-to-end not verified in CI.** The runbook (#1359) is a documentation artifact. No automated test validates the full Solr 9→10 upgrade path against a live Solr 9 data directory.
- **SolrCloud high-availability (3-node) not explicitly verified in this report.** The Solr 10 upgrade was the primary scope; HA topology testing is expected to be confirmed via CI integration tests.
- **embeddings-server and document-indexer unchanged in v2.5.0.** These services had no v2.5.0 issues and are expected to pass unchanged. Pre-release run [27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418) confirms passing.

---

## Release-Gate Conclusion

For the v2.5.0 milestone, all 13 issue-level deliverables are closed and the documented changes are coherent after aligning planned topology language to shipped behavior. The Solr 10 schema migration, single-node SolrCloud lightweight topology, Security UI, and CI/CD updates are covered by issue-closure evidence. Pre-release validation issues #1654, #1655, and #1656 are closed, and issue #1662 (OpenVINO dependency hardening) was resolved with a successful pre-release re-run at commit `a8a5cb5` ([run 27026253418](https://github.com/jmservera/aithena/actions/runs/27026253418)). The v2.5.0 release gate is clear based on pre-release evidence. If formal `ci-run.json` data becomes available, it should be appended to this report for completeness.
