# Test Report — v1.9.1

| Field       | Value                     |
|-------------|---------------------------|
| **Version** | 1.9.1                     |
| **Date**    | 2026-03-20                |
| **Runner**  | Lambert (Tester)          |
| **Verdict** | ✅ **PASS** (Conditional) |

---

## Summary

v1.9.1 validation is based on limited release preparation evidence. An issues artifact was supplied, yet this report intentionally limits itself to the strongest directly confirmed release item. The CI artifact (`ci-run.json`) and PR artifact (`prs.json`) contain no data, and the changelog artifact contains only the `[1.9.1]` header with no entries. This conservative approach is grounded in the single confirmed release item:

- **Issue #616 (verified):** aithena-ui Docker build fix — `.npmrc` not copied before `npm ci`, causing build failure.
- **CHANGELOG.md started** with v1.9.1 section header, confirming release preparation began.

No other release items can be confirmed from the supplied artifacts. No regressions are explicitly reported in the limited evidence available.

---

## Confirmed Fix in v1.9.1

### Bug Fixes
- **#616** — aithena-ui Docker build fails — `.npmrc` not copied before npm ci
  - Frontend Docker image build was failing due to missing `.npmrc` before the `npm ci` step
  - Fix ensures npm peer dependencies resolve correctly during containerized build

---

## Evidence Status

| Evidence                              | Status | Notes |
|---------------------------------------|--------|-------|
| CHANGELOG.md [1.9.1] section added   | ⚠️ Partial | Header only; no entries provided |
| PR artifact supplied                  | ⚠️ Supplied, empty | `prs.json` contains no entries |
| CI run artifact supplied              | ⚠️ Supplied, empty | `ci-run.json` contains no entries |
| Milestone closure status              | ❓ Unknown | Issues supplied, but v1.9.1 milestone not filtered |
| Test coverage metrics                 | ❓ Unknown | CI artifacts empty; no test results |
| Regressions reported                  | ✅ None | No blockers noted in available evidence |

---

## Test Coverage

No automated test results are available from the supplied CI artifact (`ci-run.json`). Test validation cannot be performed without:
- CI workflow run logs
- Per-service test counts and pass/fail status
- Coverage metrics for solr-search and document-indexer

---

## Validation Checklist

Before production deployment, operators should verify this patch-focused release:

- [ ] aithena-ui Docker image builds successfully: `docker build -t aithena-ui:1.9.1 src/aithena-ui/`
- [ ] npm ci resolves all peer dependencies (eslint configuration) during build
- [ ] Application container runs: `docker run --rm aithena-ui:1.9.1`
- [ ] Docker Compose services start without errors: `docker compose up`
- [ ] All 8 services report healthy status in health checks
- [ ] Frontend loads in browser (no MIME type errors, console clean)
- [ ] Basic search functionality works (query responds without 5xx errors)
- [ ] No new console errors in browser DevTools

---

## Notes

**Why this report is conservative:** An issues artifact was supplied, yet CI/PR/changelog detail is sparse:
- **CI run:** Artifact supplied, but empty. Cannot verify test pass/fail, coverage, or test counts.
- **PR list:** Artifact supplied, but empty. Cannot enumerate merged changes.
- **CHANGELOG:** Header only. The full release notes are not provided.
- **Issues:** Artifact supplied (200 total), but v1.9.1 milestone not filtered. Cannot confirm milestone closure.

Per instructions, we do not invent exact test counts, merged PR counts, or per-service metrics without evidence. This report is grounded only in the single confirmed release item (#616) and the absence of reported blockers.

If additional evidence (full CI logs, issue list, PR list, changelog entries) becomes available, this report should be expanded with confirmed release scope and test coverage details.

---

## Verdict

**✅ PASS** (Conditional on verification) — One critical Docker build fix (#616) is confirmed. No regressions reported in available evidence. **Recommend full artifact validation before production deployment.** The patch appears safe, but full release scope and test coverage cannot be confirmed from the supplied artifacts.
