# Test Report — v1.17.0

| Field       | Value |
|-------------|-------|
| **Version** | 1.17.0 |
| **Date**    | 2026-03-27 |
| **Runner**  | Copilot (via Squad) |
| **Verdict** | ⚠️ **Conditional** — Release evidence incomplete |

---

## Summary

The release context supplied for `v1.17.0` did **not** include captured CI results or substantive merged PR metadata:

- `prs.json` = 5 PRs (Dependabot dependency updates)
- `ci-run.json` = `[]` (empty)
- `changelog.md` contains only the version header

That means this report cannot claim a release-wide pass/fail result from upstream release evidence alone. However, the 9 closed milestone issues provide well-scoped GPU acceleration tasks and security work descriptions, which are used as the basis for spot-checks and risk assessment below.

---

## Evidence Provided

| Artifact | Status | Notes |
|---|---|---|
| `CHANGELOG.md` | ⚠️ Minimal | Contains only `## [1.17.0] — 2026-03-27` |
| `prs.json` | ✅ Present | 5 PRs: Dependabot updates for `requests` and `picomatch` |
| `ci-run.json` | ⚠️ Empty | No CI run metadata exported |
| `issues.json` | ✅ Present | 9 closed issues for milestone `v1.17.0` |

---

## Repository Spot-Checks

The following areas were identified from the closed issues as the primary change surface for v1.17.0.

### 1. GPU Support — Embeddings-Server Dependencies (#1151)

**Expected behavior after fix:**

- OpenVINO is installed in the embeddings-server image via `uv sync --extra openvino` when `INSTALL_OPENVINO=1` is set
- The resolved OpenVINO version in `uv.lock` is pinned (currently `openvino==2026.0.0`) and used by the image
- Image builds successfully on x86_64 (and ARM64 if supported)
- No import conflicts between torch and openvino
- Image size remains within budget (<300 MB added total)

**Where to verify in the repository:**

- `src/embeddings-server/Dockerfile` — should wire `INSTALL_OPENVINO` into the build and run `uv sync --extra openvino` when it is enabled
- `src/embeddings-server/uv.lock` — should include a pinned `openvino` entry (currently `openvino==2026.0.0`)

**Risk if unfixed:** Intel GPU support will not be available; only NVIDIA CUDA and CPU modes will work.

### 2. GPU Support — Device Selection Logic (#1152)

**Expected behavior after fix:**

- Embeddings-server accepts `DEVICE` environment variable (auto/cpu/cuda/xpu), default 'auto'
- Embeddings-server accepts `BACKEND` environment variable (torch/openvino), default 'torch'
- Auto-fallback logic: if `DEVICE=auto` and GPU not detected → CPU (no silent failure)
- Device selection and fallback logged at INFO/WARNING levels
- Unit tests cover device selection and fallback paths
- Backward compatible: no `DEVICE`/`BACKEND` vars set = CPU mode

**Where to verify in the repository:**

- `src/embeddings-server/` — inference logic with device/backend handling
- Test files — unit tests for device selection and fallback
- Container logs — verify device detection messages at startup

**Risk if unfixed:** GPU acceleration cannot be activated; device selection will fail silently.

### 3. GPU Support — Docker Compose Profiles (#1153)

**Expected behavior after fix:**

- `docker-compose.yml` includes `profiles: [nvidia]` on embeddings-server
- `docker-compose.yml` includes `profiles: [intel]` on embeddings-server
- NVIDIA profile: `runtime: nvidia`, GPU capability directive (`nvidia.com/gpu`)
- Intel profile: device passthrough for `/dev/dri`
- Default `docker compose up` (no GPU) still works
- Environment variables `DEVICE` and `BACKEND` flow through compose to the service

**Where to verify in the repository:**

- `docker-compose.yml` — service definitions with profiles, runtime, and device configuration
- `docker-compose.override.yml` (if present) — should not override GPU profiles
- Test with: `docker compose --profile nvidia up -d` and `docker compose --profile intel up -d`

**Risk if unfixed:** GPU device passthrough will not work; profiles will not be available.

### 4. GPU Support — NVIDIA Compose Override Example (#1154)

**Expected behavior after fix:**

- File `docs/examples/docker-compose.nvidia.override.yml` exists
- Contains full NVIDIA GPU configuration (runtime, capabilities, device settings)
- Documented usage pattern (multi-compose-file merging)
- Example environment variable setup included
- Comments explain each configuration section

**Where to verify in the repository:**

- `docs/examples/docker-compose.nvidia.override.yml` — complete, well-commented

**Risk if unfixed:** Users lack a documented copy-paste example for NVIDIA GPU setup.

### 5. GPU Documentation — User Manual (#1157)

**Expected behavior after fix:**

- `docs/user-manual.md` includes new "GPU Acceleration (v1.17.0)" section
- Performance expectations documented (2–4× NVIDIA, 1.5–2× Intel)
- NVIDIA prerequisites and setup documented (drivers, NVIDIA Container Toolkit) for Ubuntu/Debian and RHEL/CentOS
- Intel GPU setup documented (Arc GPU, compute-runtime)
- Intel CPU optimization (OpenVINO) documented
- Environment variables (DEVICE, BACKEND) documented with all values
- Docker Compose usage examples for each GPU scenario included
- Troubleshooting section covers CUDA not detected, OpenVINO fallback

**Where to verify in the repository:**

- `docs/user-manual.md` — search for "GPU Acceleration (v1.17.0)" or similar

**Risk if unfixed:** Users lack guidance for enabling GPU acceleration.

### 6. GPU Documentation — Admin Manual (#1158)

**Expected behavior after fix:**

- `docs/admin-manual.md` includes "GPU Prerequisites (v1.17.0+)" section
- NVIDIA Container Toolkit installation documented for Ubuntu/Debian, RHEL/CentOS, Windows WSL2
- Intel compute-runtime installation documented
- WSL2 GPU passthrough documented
- "GPU Troubleshooting (v1.17.0)" section added with:
  - GPU detection verification (log inspection, nvidia-smi, Intel tools)
  - CUDA version mismatch troubleshooting
  - Driver update procedures
  - GPU memory utilization monitoring guidance
  - Expected throughput metrics by hardware
  - Health check endpoints for GPU status

**Where to verify in the repository:**

- `docs/admin-manual.md` — search for "GPU" or "v1.17.0"

**Risk if unfixed:** Operators lack deployment guidance for GPU environments.

### 7. GPU Documentation — Standalone Troubleshooting Guide (#1159)

**Expected behavior after fix:**

- File `docs/guides/gpu-troubleshooting.md` exists
- Expected log messages documented for each scenario (GPU found, fallback, errors)
- Common GPU issues and solutions documented
- Hardware-specific troubleshooting (NVIDIA, Intel) included
- Performance expectations by hardware type documented
- Driver download links included
- Verification steps with driver tools documented
- FAQ section based on user feedback present
- WSL2 GPU-specific issues documented

**Where to verify in the repository:**

- `docs/guides/gpu-troubleshooting.md` — complete, comprehensive guide

**Risk if unfixed:** Users lack a dedicated resource for GPU troubleshooting.

### 8. Security — Dependency Updates (#1219, #1228, #1229, #1230)

**Expected behavior after fix:**

- `requests>=2.33.0` in `src/document-indexer/requirements.txt` or pinned dependencies
- `requests>=2.33.0` in `src/solr-search/requirements.txt` or pinned dependencies
- `requests>=2.33.0` in `src/admin/requirements.txt` or pinned dependencies
- `picomatch>=4.0.4` in `src/aithena-ui/package.json` or similar package manager
- Dependabot alerts #126–131 are resolved (GitHub shows alerts as closed)

**Where to verify in the repository:**

- Dependency manifests: `requirements.txt`, `package.json`, `pyproject.toml`
- GitHub Dependabot dashboard — alerts #126–131 marked resolved

**Risk if unfixed:** Medium-severity security vulnerabilities remain in the release.

### 9. Embeddings-Server Build Optimization (#1231)

**Expected behavior after fix:**

- Issue #1231 closed with implementation plan documented
- Proposal for separate base image (`ghcr.io/jmservera/embeddings-server-base:multilingual-e5-base`) documented
- Naming convention for base images established
- GitHub Actions workflow documented for base image updates (manual trigger or config-driven)
- Build time measurement/documentation plan included

**Where to verify in the repository:**

- GitHub issue #1231 — closed status with comments documenting next steps
- `.github/workflows/` — potential new workflow for base image builds

**Risk if unfixed:** CI builds remain slow; optimization opportunity missed.

---

## What This Report Does **Not** Prove

Because the exported release evidence is incomplete, this report does **not** prove any of the following for `v1.17.0`:

- that the release branch passed CI
- that all code PRs merged into the release
- that all repository test suites were executed for this exact release cut
- specific test counts or coverage percentages for this release

This is why the verdict is **Conditional**, not **Pass**.

---

## Risk Assessment

### Low-risk conclusions

- All 9 issues are well-described features, security updates, or documentation tasks
- No breaking API, deployment, or configuration changes are documented
- GPU support is completely opt-in via environment variables and compose profiles; existing deployments unaffected
- Dependency updates are targeted to specific services; no major version changes
- Documentation updates are additive; no content removals
- Build optimization is internal infrastructure; no user-facing changes

### Remaining risk

These gaps should be resolved by operators or release managers before making strong production claims:

- Missing CI export — cannot confirm automated tests passed for this release cut
- Empty changelog body — no authoritative summary from the release team
- Missing code PR export — cannot confirm which code changes were included beyond Dependabot updates
- GPU functionality not independently verified in test environment

---

## Recommended Validation Before Production Use

### 1. Automated validation

- [ ] Run the repository CI workflow for the exact release branch/tag
- [ ] Verify all expected test suites complete successfully (lint, unit, integration)
- [ ] Archive the CI run URL or exported summary for future release notes

### 2. GPU functionality validation (if GPU support is intended to be used)

- [ ] Build embeddings-server image and verify OpenVINO is installed
- [ ] Test `DEVICE=cuda` with NVIDIA GPU (if available) — verify CUDA is detected and used
- [ ] Test `DEVICE=auto` with no GPU — verify automatic fallback to CPU
- [ ] Test `BACKEND=openvino` (if Intel GPU available) — verify Intel GPU is detected and used
- [ ] Check container startup logs for device selection messages
- [ ] Run unit tests for device selection logic

### 3. Docker Compose GPU profiles validation

- [ ] Test `docker compose --profile nvidia up -d` (if NVIDIA available) — confirm startup and GPU access
- [ ] Test `docker compose --profile intel up -d` (if Intel available) — confirm startup and GPU access
- [ ] Test `docker compose up -d` (default) — confirm CPU mode still works
- [ ] Verify `docker compose down` and `docker compose up -d` (again) work correctly

### 4. Security validation

- [ ] Verify Dependabot alerts #126–131 are closed in GitHub
- [ ] Run SBOM (Software Bill of Materials) scan to confirm vulnerable package versions are removed
- [ ] Spot-check dependency manifest files for version pins

### 5. Documentation validation

- [ ] Confirm `docs/user-manual.md` includes GPU Acceleration section with clear setup instructions
- [ ] Confirm `docs/admin-manual.md` includes GPU Prerequisites and GPU Troubleshooting sections
- [ ] Confirm `docs/guides/gpu-troubleshooting.md` exists and is comprehensive
- [ ] Confirm `docs/examples/docker-compose.nvidia.override.yml` exists and is well-commented
- [ ] Spot-check documentation for accuracy and completeness

### 6. End-to-end integration validation

- [ ] Run a keyword search with CPU mode — confirm results are returned
- [ ] Run a semantic search with CPU mode — confirm results are returned
- [ ] (Optional, if GPU available) Run semantic search with GPU mode — verify performance improvement

---

## Deployment Readiness

**Verdict: ⚠️ CONDITIONAL**

`v1.17.0` now has complete release documentation built from 9 well-scoped closed issues and 5 Dependabot PRs. The supplied release evidence is not strong enough to declare an unconditional test pass. Production readiness should be confirmed by running or retrieving the missing CI and release-validation data, and by completing the manual validation checklist above.

---

## Recommendations

1. **Validate GPU functionality independently** — the GPU acceleration feature is a new, significant capability that should be tested in a non-production environment before production deployment.
2. **Retrieve missing CI metadata** — obtain automated test results for the release branch to confirm tests passed.
3. **Validate dependency updates** — confirm Dependabot alerts #126–131 are resolved in your GitHub security dashboard.
4. **Test documentation examples** — follow the GPU setup instructions in the updated manuals to verify they work end-to-end.
5. **Establish GPU performance baseline** — if GPU is used, run performance benchmarks (semantic search, document indexing) to establish expected improvement metrics.
6. **Do not advertise release-wide CI success** until the missing CI evidence is recovered.
