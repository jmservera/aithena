[pytest]
testpaths = .
addopts = -ra -v --timeout=600
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    indexing: indexing pipeline stress tests
    search: search latency benchmarks
    concurrent: concurrent user simulation
    docker: requires Docker daemon and running stack (skip with '-m "not docker"')
