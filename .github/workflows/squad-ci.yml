name: Squad CI
# Project type was not detected — configure build/test commands below

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Info
        run: |
          echo "Squad CI placeholder — configure build/test commands to enable:"
          echo "  Go:            go test ./..."
          echo "  Python:        pip install -r requirements.txt && pytest"
          echo "  .NET:          dotnet test"
          echo "  Java (Maven):  mvn test"
          echo "  Java (Gradle): ./gradlew test"
          exit 0
